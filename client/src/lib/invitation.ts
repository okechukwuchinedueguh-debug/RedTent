export function createRedtentInvitation(appUrl: string) {
  return `I’m using Redtent, a private space to notice your cycle, food, mood, and personal patterns with more care. Join me here: ${appUrl}`;
}
